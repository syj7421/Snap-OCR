name: Build and Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        include:
          - os: ubuntu-latest
            build_command: pyinstaller --onefile main.py
            artifact_name: ubuntu-Snap-OCR-executable
          - os: macos-latest
            build_command: python setup.py py2app
            artifact_name: macos-Snap-OCR-executable
          - os: windows-latest
            build_command: pyinstaller --onefile main.py
            artifact_name: windows-Snap-OCR-executable

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller py2app

    - name: Build executable
      run: ${{ matrix.build_command }}

    - name: Upload artifact
      uses: actions/upload-artifact@v2
      with:
        name: ${{ matrix.artifact_name }}
        path: dist/*

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
    - name: Download Ubuntu artifacts
      uses: actions/download-artifact@v2
      with:
        name: ubuntu-Snap-OCR-executable
        path: ./ubuntu-Snap-OCR-executable

    - name: Download macOS artifacts
      uses: actions/download-artifact@v2
      with:
        name: macos-Snap-OCR-executable
        path: ./macos-Snap-OCR-executable

    - name: Download Windows artifacts
      uses: actions/download-artifact@v2
      with:
        name: windows-Snap-OCR-executable
        path: ./windows-Snap-OCR-executable

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}  # Ensure the correct reference to the tag name
        release_name: Release ${{ github.ref_name }}  # Correct reference
        draft: false
        prerelease: false

    - name: Upload Ubuntu Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./ubuntu-Snap-OCR-executable/*
        asset_name: ubuntu-Snap-OCR-executable
        asset_content_type: application/octet-stream

    - name: Upload macOS Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./macos-Snap-OCR-executable/*
        asset_name: macos-Snap-OCR-executable
        asset_content_type: application/octet-stream

    - name: Upload Windows Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.PAT_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./windows-Snap-OCR-executable/*
        asset_name: windows-Snap-OCR-executable
        asset_content_type: application/octet-stream
